import { Section } from '../components/Section';

function Home() {
  return (
    <main>
      <Section.Root>
        <Section.Title headline="Sobre mim" />
      </Section.Root>
    </main>
  );
}

export default Home;
