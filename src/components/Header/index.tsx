import HeaderBurguerButton from './HeaderBurguerButton';
import HeaderDarkModeButton from './HeaderDarkModeButton';
import HeaderLinkList from './HeaderLinkList';
import HeaderRoot from './HeaderRoot';

export const Header = {
  Root: HeaderRoot,
  LinkList: HeaderLinkList,
  BurguerButton: HeaderBurguerButton,
  DarkModeButton: HeaderDarkModeButton,
};
